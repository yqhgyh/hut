int main()
{
	int i = 0;
	int j = 0;
	for (i = 0; i <= 20; i++)
	{
		for (j = 0; j <= 20; j++)
		{
			if ((i - 10) * (i - 10) + (j - 10) * (j - 10) <= 100 && (i - 10) * (i - 10) + (j - 10) * (j - 10) >= 64)
			{
				printf("$");
			}
			else
			{
				printf(" ");
			}
		}
		printf("\n");
	}
	return 0;
